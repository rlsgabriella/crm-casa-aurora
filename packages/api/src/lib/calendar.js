// Google Calendar integration — placeholder para Fase 2
export async function criarEvento({ titulo, inicio, fim, descricao }) {
  // TODO: implementar OAuth2 Google Calendar
  console.log('Google Calendar: criarEvento não implementado ainda', { titulo, inicio, fim, descricao })
  return null
}
